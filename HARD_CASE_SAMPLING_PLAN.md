# 桃园困难地区样本增加方案

目标是提高建成区与裸地、农地之间的区分能力，并改善跨年份迁移。困难样本要优先覆盖“模型容易错的边界”，而不是继续随机撒点。

## 一、先固定 30 m 判读规则

- 一个 Landsat 像元是 30 m × 30 m，面积约 900 m²。
- 标注框内某一类别占主体（超过约 50%）时，标为该类。
- 没有类别超过一半时标为 MIXED，不把它强行变成训练标签。
- 影像无法判断时标为 UNCERTAIN。
- 训练样本尽量选纯净像元：目标类别应覆盖框内至少约 70%，并与其他类别边界保持至少一个像元距离。

## 二、困难地区分层

建议新增 60–80 个多边形或等量纯净像元，按下列方向分层，每一组都记录来源位置、日期和困难类型：

| 困难类型 | 重点区域 | 建议新增 |
| --- | --- | ---: |
| 裸地—建成区 | 工地、堆土、裸露停车场、厂房屋顶 | 15–20 |
| 农地—裸地 | 收割后农田、温室周边、旱地和施工边缘 | 15–20 |
| 建成区—植被 | 城市边缘、低密度住宅、校园和公园 | 15–20 |
| 水体—阴影/湿地 | 河岸、水库边缘、桥下阴影、养殖池 | 8–10 |
| 山地—开发地 | 山麓道路、坡地裸岩、采石场 | 8–10 |

每类至少保留 10 个新增样本，避免只改善一个局部混淆对。

## 三、采样位置和时间

1. 先用当前 2020 分类结果的混淆矩阵定位错误最多的类别对。
2. 在桃园机场、工业区、城市边缘、河岸、工地和山麓各选空间分散的样本。
3. 每个候选位置同时查看 2020 年 Landsat 合成和更高分辨率参考影像。
4. 对跨年份训练，优先补充 2014、2020、2025 都能判读的稳定地物；季节性农地要单独记录季节。
5. 同一地点周围 300 m 内不要重复采样，防止训练和验证再次空间泄漏。

## 四、采样表字段

每个样本至少保留：

- sample_id
- landcover
- difficulty_type
- reference_year
- source_image
- purity_estimate
- review_status
- notes

建议把困难样本单独保存为 data/hard_cases_training.geojson，不要覆盖第一版 93 个基线多边形，这样可以比较改样本前后的模型变化。

## 五、验收标准

新增样本后重新训练，并用原来的独立验证点评价，不能把验证点转成训练样本。接受新模型至少要满足：

- 建成区精确率不低于当前 89.4%；
- 建成区召回率从 70.7% 提升到至少 80%；
- 裸地精确率和召回率都明显高于当前 18.8% 和 11.2%；
- 2014、2020、2025 的结果不再出现无法解释的方向反转；
- 在困难地区单独报告混淆矩阵，不只报告总体精度。

如果召回率提高但建成区面积明显过高，说明模型把裸地或农地吞进了建成区，需要回到困难样本分层继续补样本，而不是直接接受结果。
