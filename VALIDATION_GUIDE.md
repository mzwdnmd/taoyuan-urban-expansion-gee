# 2020 验证点判读任务

目标：完成250个桃园市独立验证点，为作品集提供可信的区域精度结果。

1. 运行 `validation_map_server.py`，在自动打开的地图页面上判读。每次只做25点，约15–25分钟；共10组。
2. 地图会用方框标出完整的30 m × 30 m像元。根据 Sentinel-2 2020 合成影像判断方框内主体类别，点击：`0` 水体、`1` 植被、`2` 农地、`3` 裸地、`4` 建成区。
3. 某一类覆盖超过方框一半时，标为该类；没有任何一类超过50%时选择“混合像元”。影像本身看不清才选择“暂时无法判断”。这两类记录不会被强行计入精度矩阵。
4. 按钮会自动保存到 `results/validation_labels_2020.json` 并跳到下一点。使用“从第1个开始复核”可以检查方框上线前完成的记录，不会清空原标注。
4. 全部完成后运行 `score_validation.py`；程序会计算面积加权总精度、95%置信区间、各类别精确率/召回率和校正面积。

判读规则：以点所在的30米像元主体覆盖为准。道路、建筑和其他明显不透水面记为建成区；正在施工且大部分为裸土记为裸地；规则一旦确定，全部点保持一致。
